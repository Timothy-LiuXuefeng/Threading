# Threading

C++ thread-related library  

namespace: `::timothy`

+ thread-pool: `thread_pool`  
+ semaphore: `semaphore`  
+ concorrent containers (namespace: `::timothy::concurrent`)  

  + `concurrent_queue`
